import { serve, json } from "../_shared/http.ts"; import { requireMembership } from "../_shared/auth.ts";
serve(async(req)=>{const {groupId,email,permissionLevel="read_write"}=await req.json();const {admin,user,membership}=await requireMembership(req,groupId,true);if(membership.role!=="owner")throw new Error("Somente o titular pode convidar");const token=crypto.randomUUID()+crypto.randomUUID();const expires=new Date(Date.now()+7*86400000).toISOString();const {error}=await admin.from("group_invitations").insert({group_id:groupId,invited_email:String(email).toLowerCase(),invited_by:user.id,permission_level:permissionLevel,token,expires_at:expires});if(error)throw error;const resend=Deno.env.get("RESEND_API_KEY");const appUrl=Deno.env.get("APP_URL");if(!resend||!appUrl)return json({created:true,emailSent:false,reason:"Configure RESEND_API_KEY e APP_URL"});const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${resend}`,"Content-Type":"application/json"},body:JSON.stringify({from:Deno.env.get("RESEND_FROM")||"StepFin <convites@stepfin.app>",to:[email],subject:"Você foi convidado para o StepFin",html:`<p>Você recebeu um convite para organizar as finanças em conjunto.</p><p><a href="${appUrl}/accept-invite?token=${encodeURIComponent(token)}">Aceitar convite</a></p><p>O convite expira em 7 dias.</p>`})});if(!response.ok)throw new Error("Convite criado, mas o e-mail não pôde ser enviado");return json({created:true,emailSent:true});});
